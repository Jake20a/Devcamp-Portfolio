require 'rspec'




def bubble_sort array 
  array.each do
    swap_count = 0
    array.each_with_index do |a, index|
end

p =  arr = [4, 1, 6, 10, 44, 2, 3]


describe 'Adding bubble sort to the Array class' do
  it 'Properly sorts an array' do
    arr = [4, 1, 6, 10, 44, 2, 3]
    expect(arr.bubble_sort).to eq(arr.sort)
  end
end



def bubble_sort array 
  array.each do
    swap_count = 0
    array.each_with_index do |a, index|
     arr =  [4, 1, 6, 10, 44, 2, 3] 
  end
end
  

  p = array
end


require 'rspec'

class = Array 
 def bubble_sort
   n = self.length
   
    loop do
        swapped =  FALSE
        
      (n-1).times do |i|
        if self|i| > self|i+1|
      
    end
    
      break if not swapped
    end
  end
end

describe 'Adding bubble sort to the Array class' do
  it 'Properly sorts an array' do
    arr = [4, 1, 6, 10, 44, 2, 3]
    expect(arr.bubble_sort).to eq(arr.sort)
  end
end